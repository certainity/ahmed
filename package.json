{
  "name": "drive-movies-cinema",
  "private": true,
  "version": "1.0.0",
  "description": "A private movie-streaming React app that lists and streams videos from one approved Google Drive folder tree.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c auto \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "lint:server": "node --check server/index.js",
    "lint:client": "npm run build --workspace client",
    "lint": "npm run lint:server && npm run lint:client"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
